/**
 * Facts the legal pages state. Kept here so there is one place to correct
 * when any of them stops being true.
 */

/**
 * Revision date of the Terms and Privacy text — the date the wording last
 * changed, edited by hand. Not a data-ingestion date.
 */
export const LEGAL_LAST_UPDATED = "17 September 2026";

/** Shown in section 8 of both documents. MUST be filled in before deploying. */
export const CONTACT = "[add a contact email]";

export const BUILT_BY = "Finn Cox";

/** Named as processors in the privacy policy. Update if either changes. */
export const HOSTING_PROVIDER = "Vercel";
export const DATABASE_PROVIDER = "Neon";
